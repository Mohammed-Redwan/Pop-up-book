// using System.Collections;
// using System.Collections.Generic;
// using UnityEngine;

// public class ScalingInBook : MonoBehaviour
// {
//     public float scalingAfter90;
//     // public float scalingSpeed = 100f;
//     // Start is called before the first frame update
//     void Start()
//     {
        
//     }

//     // Update is called once per frame
//     void Update()
//     {
//         if (Input.GetAxisRaw("Horizontal") > 0 && RotateOnOpen.inBookAngel > 90 && scalingAfter90 < 20) {
//             scalingAfter90 += Time.deltaTime * scalingSpeed;
//             transform.localScale += new Vector3(0, 0, Time.deltaTime * scalingSpeed);
//         }
//     }
// }
